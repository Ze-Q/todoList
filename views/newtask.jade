extends layout

block content
    h1= title
    form#formAddTask(name="addtask",method="post",action="/addtask")
        input#inputNewTask(type="text", placeholder="New Task", name="task")
        button#btnSubmit(type="submit") submit